{
  "name": "ludmyspin",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "echo 'Deploy: no TypeScript compilation needed'",
    "simulate": "tsx scripts/simulate.ts",
    "reconcile": "tsx scripts/reconcile.ts"
  },
  "dependencies": {
    "tsx": "^4.11.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.4.0"
  }
}
